[package]
name = "dreg"
version.workspace = true
authors.workspace = true
edition.workspace = true
homepage.workspace = true
repository = "https://github.com/rtthw/dreg/dreg"
license.workspace = true

[features]
default = ["term"]
term = ["dep:dreg-term"]
wasm = ["dep:dreg-wasm"]

[dependencies]
dreg-core = { workspace = true }
dreg-term = { version = "0.3.0", path = "../dreg-term", optional = true }
dreg-wasm = { version = "0.3.0", path = "../dreg-wasm", optional = true }
